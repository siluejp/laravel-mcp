# タスクリスト: Laravel 5.6 コーディング支援AIエージェント

**入力**: `/specs/001-laravel-5-6/` からの設計ドキュメント

## フェーズ1: セットアップ (共有インフラ)

**目的**: プロジェクトの初期化と基本構造の構築

- [x] T001 `pipenv` または `poetry` を使用してPythonプロジェクトを初期化する (`pyproject.toml`)
- [x] T002 [P] 主要な依存関係を追加する: `fastmcp`, `pydantic`, `uvicorn` (`pyproject.toml`)
- [x] T003 [P] 開発用の依存関係を追加する: `pytest`, `ruff` (`pyproject.toml`)
- [x] T004 [P] `ruff` をリンターおよびフォーマッターとして設定する (`ruff.toml` または `pyproject.toml`)
- [x] T005 基本的な `src` ディレクトリ構造を作成する (`src/main.py`, `src/core/`, `src/tools/`)
- [x] T006 コンテナ化された開発と本番用の `Dockerfile` を作成する

---

## フェーズ2: 基盤構築 (ブロッキング前提条件)

**目的**: 全てのユーザーストーリー実装前に完了が必要なコアインフラの構築

- [x] T007 `src/main.py` に基本的なFastMCPサーバーのセットアップを実装する
- [x] T008 `src/core/database.py` と `src/core/models.py` に、SQLAlchemy等を使用してデータベース接続と `Conversation` モデルを実装する
- [x] T009 `alembic` を使用して初期データベーススキーママイグレーションを作成する
- [x] T010 **(RAGセットアップ)** Laravel 5.6ドキュメントをクローリングするためのスクリプト `scripts/ingest_docs.py` を作成する
- [x] T011 **(RAGセットアップ)** [T010に依存] `ingest_docs.py` にテキスト抽出とチャンキングのロジックを実装する
- [x] T012 **(RAGセットアップ)** [P] RAGの依存関係を追加する: `langchain`, `beautifulsoup4`, `requests`, `faiss-cpu` (`pyproject.toml`)
- [x] T013 **(RAGセットアップ)** [T011に依存] `ingest_docs.py` にエンベディング生成とベクトルストアをディスクに保存するロジックを実装する
- [x] T014 **(RAGセットアップ)** [T013に依存] `ingest_docs.py` を実行してベクトルストアのアーティファクトを作成する
- [x] T015 `src/core/rag_service.py` に、ベクトルストアをロードして関連ドキュメントを検索できるRAG検索サービスを実装する

---

## フェーズ3: ユーザーストーリー1 - Eloquentクエリ生成 (P1) 🎯 MVP

**ゴール**: 開発者が複雑なEloquentクエリを作成するのを支援する
**独立したテスト**: 自然言語でDBモデルに関するクエリを渡し、エージェントが有効なEloquentクエリ文字列を返すことを確認する

- [x] T016 [P] [US1] `src/tools/laravel_assistant.py` に `laravel_5_6_assistant` ツールの基本構造を作成する
- [x] T017 [US1] [T015, T016に依存] ツール内にUS1の中核ロジックを実装する。ユーザーのクエリを受け取り、RAGサービスでコンテキストを取得し、特定のプロンプトでLLMを使用してEloquentクエリを生成する
- [x] T018 [US1] [T008, T017に依存] US1のクエリと応答をSQLiteデータベースにロギングする機能を実装する
- [x] T019 [US1] [T007, T016に依存] `src/main.py` でFastMCPサーバーにツールを登録する

---

## フェーズ4: ユーザーストーリー2 - エラーメッセージのデバッグ (P2)

**ゴール**: 開発者がエラーメッセージをデバッグするのを支援する
**独立したテスト**: 一般的なLaravelのエラーメッセージと関連コードを与え、エージェントが正しい原因を提案することを確認する

- [x] T020 [P] [US2] [T017に依存] デバッグシナリオを処理するために `laravel_5_6_assistant` ツールのプロンプトを強化する
- [x] T021 [US2] [T017に依存] 入力が機能リクエストかエラーメッセージかを検出するロジックをツールのコアロジックに更新する
- [x] T022 [US2] [T018に依存] US2のクエリと応答のロギングを実装する

---

## フェーズ5: ユーザーストーリー3 - 概念の説明 (P3)

**ゴール**: Laravel 5.6の概念を説明する
**独立したテスト**: Laravelのコア概念について質問し、回答が明確で正確であることを確認する

- [x] T023 [P] [US3] [T020に依存] 概念に関する質問のために `laravel_5_6_assistant` ツールのプロンプトをさらに強化する
- [x] T024 [US3] [T021に依存] RAGサービスに大きく依存して概念的な質問を処理するようにツールのコアロジックを更新する
- [x] T025 [US3] [T022に依存] US3のクエリと応答のロギングを実装する

---

## フェーズN: 仕上げと横断的関心事

- [x] T026 [P] ツールとサーバーに包括的なエラーハンドリングを追加する
- [ ] T027 [P] RAGサービスとツールロジックのユニットテストを作成する (`tests/`)
- [ ] T028 `quickstart.md` を最終的なテスト済みの手順で更新する
- [ ] T029 すべてのコードが `ruff` のリンティングルールに準拠しているかレビューする
